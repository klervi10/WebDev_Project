// area.model.ts 

// TypeScript interfaces are used to declare the expected form (structure) of an object. 
// In this case, the Area interface defines the expected structure for an object representing an area.
export interface Area {
    name: string;
    imageSrc: string;
    buttonLabel: string;
}
