// meals.model.ts

// TypeScript interfaces are used to declare the expected form (structure) of an object. 
// In this case, the Meals interface defines the expected structure for an object representing a meal.
export interface Meals {
  name: string;
  img: string;
  description: string;
}
