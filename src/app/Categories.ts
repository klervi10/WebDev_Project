// categories.model.ts (exemple)

// TypeScript interfaces are used to declare the expected form (structure) of an object. 
// In this case, the Categories interface defines the expected structure for an object representing a category.
export interface Categories {
        name: string;
        imageSrc: string;
        buttonLabel: string;
    }
    